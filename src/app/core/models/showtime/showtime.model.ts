export interface Showtime {
  id: number;
  movieId: number;
  roomId: number;
  roomName: string;
  cinemaId: number;
  showDate: string;
  startTime: string;
  ticketPrice: number; // Giá vé (VD: 80000)
}
